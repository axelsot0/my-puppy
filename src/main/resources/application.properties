# ===========================================
# My Puppy - Generic Booking Platform API
# ===========================================

# --- HTTP ---
quarkus.http.port=${PORT:8080}
quarkus.http.host=0.0.0.0

# --- CORS ---
quarkus.http.cors=true
quarkus.http.cors.origins=/.*/

# --- Datasource: Dev/Test (H2) ---
%dev.quarkus.datasource.db-kind=h2
%dev.quarkus.datasource.jdbc.url=jdbc:h2:mem:mypuppydb;DB_CLOSE_DELAY=-1
%dev.quarkus.datasource.username=sa
%dev.quarkus.datasource.password=

%test.quarkus.datasource.db-kind=h2
%test.quarkus.datasource.jdbc.url=jdbc:h2:mem:mypuppydb;DB_CLOSE_DELAY=-1
%test.quarkus.datasource.username=sa
%test.quarkus.datasource.password=

# --- Datasource: Production (PostgreSQL) ---
%prod.quarkus.datasource.db-kind=postgresql
%prod.quarkus.datasource.jdbc.url=jdbc:postgresql://${PGHOST}:${PGPORT:5432}/${PGDATABASE}
%prod.quarkus.datasource.username=${PGUSER}
%prod.quarkus.datasource.password=${PGPASSWORD}

# --- Hibernate ORM ---
quarkus.hibernate-orm.database.generation=drop-and-create
%prod.quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=true
%prod.quarkus.hibernate-orm.log.sql=false

# --- JWT ---
mp.jwt.verify.publickey.location=publicKey.pem
mp.jwt.verify.issuer=my-puppy
smallrye.jwt.sign.key.location=privateKey.pem
%prod.mp.jwt.verify.publickey=${JWT_PUBLIC_KEY}
%prod.smallrye.jwt.sign.key=${JWT_PRIVATE_KEY}
smallrye.jwt.new-token.issuer=my-puppy

# --- Super Admin (bootstrap) ---
mypuppy.super-admin.email=${SUPER_ADMIN_EMAIL:admin@mypuppy.com}
mypuppy.super-admin.password=${SUPER_ADMIN_PASSWORD:admin123}
mypuppy.super-admin.name=${SUPER_ADMIN_NAME:Platform Admin}

# --- OpenAPI / Swagger UI ---
quarkus.swagger-ui.always-include=true
quarkus.smallrye-openapi.info-title=My Puppy API
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.smallrye-openapi.info-description=Generic multi-tenant booking platform API

# --- Logging ---
%dev.quarkus.log.category."com.mypuppy.application.service.LoginOtpService".level=DEBUG

# --- OTP / Email ---
mypuppy.otp.expiration-minutes=${OTP_EXPIRATION_MINUTES:5}
mypuppy.otp.max-attempts=${OTP_MAX_ATTEMPTS:5}
mypuppy.otp.mail.from=${OTP_MAIL_FROM:no-reply@mypuppy.com}
mypuppy.otp.mail.subject=${OTP_MAIL_SUBJECT:Your My Puppy OTP code}

# --- OTP Rate Limiting ---
mypuppy.otp.rate-limit.max-per-window=${OTP_RATE_LIMIT_MAX:5}
mypuppy.otp.rate-limit.window-minutes=${OTP_RATE_LIMIT_WINDOW:15}

# --- OTP Cleanup ---
mypuppy.otp.cleanup.cron=${OTP_CLEANUP_CRON:0 0 * * * ?}

# --- Mailer: Dev/Test (mock) ---
%dev.quarkus.mailer.mock=true
%test.quarkus.mailer.mock=true

# --- Mailer: Production (SMTP) ---
%prod.quarkus.mailer.from=${SMTP_FROM:no-reply@mypuppy.com}
%prod.quarkus.mailer.host=${SMTP_HOST}
%prod.quarkus.mailer.port=${SMTP_PORT:587}
%prod.quarkus.mailer.start-tls=REQUIRED
%prod.quarkus.mailer.username=${SMTP_USERNAME}
%prod.quarkus.mailer.password=${SMTP_PASSWORD}
